node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local

# macOS
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# Claude settings (pueden contener tokens)
.claude/settings.json

# Scripts de seed (contienen keys) — solo la carpeta de la raíz, no
# backend/scripts (ahí vive exportWindowsRootCerts.ts, que sí se versiona)
/scripts/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.vercel
.env*

# .env.production SÍ se versiona (a propósito, ver CLAUDE.md "APK/OTA sin
# datos"): @expo/env carga este archivo automáticamente en cualquier
# build/update en modo producción (NODE_ENV=production — cubre los perfiles
# "preview" y "production" de eas.json), con prioridad sobre .env local, sin
# depender de --environment ni de acordarse de nada. No tiene secretos, son
# variables EXPO_PUBLIC_* que igual terminan visibles en el bundle del cliente.
!.env.production
